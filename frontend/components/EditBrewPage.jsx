// Create a page that shows BrewForm in Edit mode.
// Pre-fill form with brew data from /api/brews/:id.
// Has Delete button that deletes brew then redirects to Brew Log.
// Save button sends PUT to /api/brews/:id.
